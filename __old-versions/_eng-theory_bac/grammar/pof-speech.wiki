самостоятельные:
  verb(глагол):
    по функциям:
      main(смысловые)
        %% to get отдельная статья
        %%   +разговорные глаголы
      aspectual(аспектуальные):
        %% характер протекания действия
      auxiliary(вспомогательные):
        to be
          %% check отдельная статья
        to do
          %% check отдельная статья
        to have
          %% check отдельная статья
        %% для сложных:
        %%   should/would
        %%     future in the past
        %%     сослагательное наклонение в if-предложениях 2,3 типа s1p1
      semi-auxiliary(полувспомогательные):
        linking(связки):
          = самые популярные? =
          = классификация? =
        == каузативные глаголы/обороты ==
        modal(модальные):
          ==== skip shall ====
          ==== skip might ====
          ==== skip ought to ====
          %% частица "to" не используется
          = разделить на прямое и вероятностное значение =
          == ВЕРОЯТНОСТНОЕ/прямое ЗНАЧЕНИЕ ==
            = в вероятностном значении время передает инфинитив =
              == инфинитив может быть в разных формах? ==
          == +модальные как способ выражения будущего ==
          = проверить на вспомогательный глагол в вопросах =
          can(мочь,уметь):
            can, could, will be able to
          may(возможно):
            may, might, will be allowed to
          %% might(есть вероятность):
          %%   might, NONE, NONE
          should(рекомендация):
            should, have should, should
          = британский should: =
            %% shall(должен, спросить совет, дать указ):
            %%   shall, should, NONE
          will(намереваться):
            will/would(вежливо), would, will
          must(запрет):
            must, have to, have to
        semi-modal(полумодальные):
          %% частица "to" не используется
          to be(должно по соглашению):
            be to, to be, will have to
          have to(необходимость):
            have to, have to, have to
          %% ought to(моральное обязательство):
          %%   ought to, NONE, NONE
          need to(нужно):
            need to, needed to, will need to
          let(давайте):
            let, let, will let
          dare(сметь):
            inf-to,q-aux: dare, dared, NONE
        модальные констукции:
          %% to be:
          %%   am/is/are - будущее из настоящего
          %%   was/were - будушее из прошлого
          = check частица "to" =
          = проверить вариации +have =
            = это перфектный инфинитив =
          be able to(иметь возможность):
            be able to, was/were able to, will be able to
          be allowed to(разрешено):
            be allowed to, was/were allowed to, will be allowed to
          %% be about to
          to be going to/gonna(собираться что-то сделать):
            to be going to/gonna, was/were going to/gonna, will be going to/gonna
          %% be likely to
          be supposed/expected/projected to(предполагается):
            be supposed to, was/were supposed to, will have to
          %% be bound to
          to be/get used to(привыкать):
            to be/get used to, to be/get used to, to be/get used to
          = to be due to – должно произойти =
          = to be sure to – =
          = to be set to – быть настроенным сделать что-то =
          have better(лучше бы, следовало бы):
            had better, should, NONE
          %% have got(лучше бы, следовало бы)
          %% have best(лучше бы, следовало бы)
          %% mean to
          %% seem to
          %% tend to
          used to(регулярное действие в прошлом, но больше не делается):
            NONE, used to, NONE
    правильность:
      regular
      irregular
    finite-forms(личные/неличные формы):
      finite
      non-finite:
        participle(причастие):
          participle 1 - v2
          participle 2 - v3
        = inf(инфинитив) =
        = gerund(герундий) =
    activity:
      active/dynamic(действия)
      state/stative(состояния):
        usage:
          continuous - для эмоциональной окраски
          simple - other
    transitive(переходность):
      intransitive:
        %% не используется с дополнением
          = только прямым? =
        %% не используется с passive voice
      transitive:
        %% требует дополнения
          = только прямого? =
        однопереходные: прямое дополнение
        двупереходные: прямое + косвенное дополнениe
        сложнопереходные: прямое дополнениe + определение
          = почему тут всегда есть прямое дополнение? =
    constructions:
      simple(только глагол)
      prepositional(предложные):
        %% verb + prepositional
        %% смысл сохраняется
        transitive:
          inseparable
      phrasal(фразовые):
        %% verb + adverb/prepositional
        %% смысл меняется
        intransitive:
          inseparable
        transitive:
          inseparable
          separable
      phrasal-prepositional(фразово-предложные):
        %% verb + adverb + prepositional
        %% смысл меняется
        = check ruls. it's the same? =
    splitting:
      inseparable(неразделяемые)
      separable(разделяемые):
        prepositional location:
          до(inseparable): interrogative mood
          после: меcтоимение (him, her, them, us, it)
          до/после: остальные случаи
          инверсия: imperative mood
          = сложное дополнение =
      double-f(и разделяемый и неразделяемый):
        потому что один и тот же глагол имеет несколько значений
  Noun(существительное):
    Names(по именам):
      Нарицательные (Common Names)
      Собственные (Proper Names)
    Gender(по роду):
      -ine/-ess мужской в женский
    Plural(по числам):
      Исчисляемые (Countable Nouns)
        simple
        только в единственном числе
        только в множественном числе
      Собирательные (Collective Nouns)
      Неисчисляемые (Uncountable Nouns)
    == Case(падеж): ==
      = common(общий) =
      = possessive(притяжательный) =
    == determinative(детерминатив/определитель): ==
      identifiers(идентификаторы):
        articles(артикли)
        demonstratives(указатели)
          %% указательных местоимений
        притяжательные(possessives)
          %% существительных в притяжательном падеже
          %% притяжательными местоимениями
        interrogatives(вопросительные слова)
          %% вопросительных местоимений
        indefinite(неопределенные идентификаторы)
          %% неопределенных местоимений
      quantifiers(квантификаторы):
        определенные квантификаторы (definite)
          %% количественных числительных
        неопределенные квантификаторы (indefinite)
          %% неопределенных местоимений
  = Pronoun(местоимение): =
    == case: ==
      subjective case (прямой падеж)
      objective case (косвенный падеж)
      possessive case (притяжательный падеж)
    types:
      == another? ==
        == get full list for each ==
      Личные:
      Притяжательные:
      = отрицательные =
      = вопросительные =
        = +вопросительные конструкции =
      Количественные (Quantitative pronouns)
      Указательные:
      Возвратные:
      Взаимные:
      Относительные:
      Неопределенные:
      Разделительные:
      Универсальные:
  = Numeral(числительное): =
    %% дроби,десятки,прочие
  = Participle(причастие): =
    %% перевод из части речи в другую:
    %% модификации:
    %%    = существительное<-существительное =
  = Adverb(наречие): =
    %% перевод из части речи в другую:
    %%     наречие<-прилагательное:
    %%       -ly/-iaclly
    %% модификации:
    %%    = существительное<-существительное =
  = Adjective(прилагательное): =
    %% +complex
    %% словообразование:
    %%   Степени сравнения прилагательных и наречий
    %%   сравнительные степени
    %%   перевод из части речи в другую:
    %%   модификации:
    %%     = существительное<-существительное =
служебные:
  = Preposition(предлог): =
    == 40(100) штук - вызубрить когда какой использовать, толкования ==
      для каждого:
        паттерны когда употребляется:
          формула - пример
        устойчивые выражения
        фразовые глаголы
  == Conjunction(союз): ==
  = Article(артикль): =
    %% == determeners: ==
      %% правила:
      %%   единственное число:
      %%     a(an)
      %%     the
      %%     уточняющее местоимение
      %%     колличество: one
      %%   plural(множественное число):
      %%     без артикля
      %%     the
      %%     уточняющее местоимение
      %%     колличество: число или some, any, a lot of, etc..
      %%   неисчисляемое:
      %%     без артикля
      %%     the
      %%     уточняющее местоимение
      %%     колличество: МЕРА или some, any, a lot of, etc..
      %% исключения:
      %%   == выучить ==
      %% = в каких случаях в предложении использовать? =
  == Paticle(частица): ==
  = Interjection(междометие): =
